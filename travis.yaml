language: generic

services:
  - docker

before_install:
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin

script:
  # Build Docker images
  - docker build -t udagram-api-feed ./udagram-api-feed
  - docker build -t udagram-api-user ./udagram-api-user
  - docker build -t udagram-frontend ./udagram-frontend
  - docker build -t reverseproxy ./reverseproxy

  # Tag Docker images
  - docker tag udagram-api-feed ductu1305/udagram-api-feed
  - docker tag udagram-api-user ductu1305/udagram-api-user
  - docker tag udagram-frontend ductu1305/udagram-frontend:local
  - docker tag reverseproxy ductu1305/reverseproxy

after_success:
  # Push Docker images to DockerHub
  - docker push ductu1305/udagram-api-feed
  - docker push ductu1305/udagram-api-user
  - docker push ductu1305/udagram-frontend:local
  - docker push ductu1305/reverseproxy